using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    // so the plan is for each gear to have a next and prev to determine it's rotation as well as known teeth number and speed i guess.
    // can I give them these properties
    //public float teethNumber;
    //public Sprite prevGear;
    //public Sprite nextGear;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
